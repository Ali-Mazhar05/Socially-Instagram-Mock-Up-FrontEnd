<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white">

    <!-- Center container -->
    <LinearLayout
        android:id="@+id/container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"

        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="34dp">

        <!-- App Title -->
        <TextView
            android:id="@+id/appTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Socially"
            android:textSize="40sp"
            android:textStyle="bold"
            android:textColor="#9C4C42"
            android:layout_marginBottom="36dp" />


        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profileImage"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:src="@drawable/main_profile_circle"
            android:layout_marginBottom="12dp" />

        <!-- Username -->
        <TextView
            android:id="@+id/tvUsername"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="jacob_w"
            android:textSize="16sp"
            android:textColor="@android:color/black"
            android:layout_marginBottom="16dp" />

        <!-- Login Button -->
        <TextView
            android:id="@+id/btnLoginSwitch"
            android:layout_width="300dp"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/switch_accounts_login"
            android:backgroundTint="#9C4C42"
            android:text="Log in"
            android:textColor="@android:color/white"
            android:textStyle="bold"
            android:gravity="center" />

        <!-- Switch Accounts -->
        <TextView
            android:id="@+id/switchAccounts"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Switch accounts"
            android:textStyle="bold"
            android:layout_marginTop="10dp"
            android:textSize="14sp"
            android:textColor="#9C4C42"
            android:layout_marginBottom="36dp" />
    </LinearLayout>

    <!-- Divider -->
    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_above="@+id/signupPromptSwitch"
        android:background="#CCCCCC"
        android:layout_marginBottom="8dp" />

    <!-- Signup Section pinned to bottom -->
    <TextView
        android:id="@+id/signupPromptSwitch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        android:text="@string/switch_accounts_signuptext"
        android:textSize="14sp"
        android:textColor="#9C4C42"
        android:padding="16dp" />

</RelativeLayout>
